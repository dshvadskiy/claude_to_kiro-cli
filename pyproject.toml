[project]
name = "claude-to-kiro-cli"
version = "0.1.0"
description = "Converts Claude agents and Skills into Kiro compatible format"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
]
